<div>
    {% if posts|length > 0 %}
        {% for post in posts %}
            {{ include('post/index.html.twig', {post: post}) }}
                <button style="background-color: #AAAAAA"><a href="{{ path('post_add' ) }}"> Add new Post</a></button>
                <button><a href="{{ path('app_logout') }}"> Logout</a></button>
        {% endfor %}
    {% else %}
        There's nothing here yet!
    {% endif %}
</div>