{{ form_start(form) }}
<div>{{ form_errors(form) }}</div>
<div>
    {{ form_label(form.text) }}
</div>
<div>
    {{ form_widget(form.text, {'attr': {'style': 'color: blue;'}}) }}
</div>
{{ form_end(form) }}